# Raw data: 208 MB, CC BY 4.0, downloaded from UCI. Kept out of git.
data/raw/
*.csv
*.duckdb
*.parquet

# The life table is the deliverable of layer 5, not bulk data: 4 events and one
# suspension. It is small, it is derived, and it must be diffable.
!data/events.csv

# Environment and derived artefacts
.venv/
__pycache__/
plots/*.png
vector_db/
traces/

# Session working notes: the running log of what we checked, corrected and withdrew
# while building this. Kept on disk, deliberately not published. The durable technical
# content lives in docs/data-quality.md and docs/signal-and-events.md.
docs/correction-*.md
docs/findings-*.md
docs/design-sketch-*.md
